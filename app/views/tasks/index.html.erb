<h1> Task Manager </h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, show_task_path(task) %> <%= link_to "🗑️", delete_task_path(task), method: :delete, data: { confirm: "Are you sure?" } %> </li>
  <% end %>
</ul>
<%= link_to "New task", new_task_path %>
